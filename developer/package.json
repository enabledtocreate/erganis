{
  "name": "@erganis/module-developer",
  "version": "0.1.0",
  "private": true,
  "description": "Erganis Developer tooling module — separate repo, not shipped by default",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@erganis/platform": "file:../core/packages/typescript"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
