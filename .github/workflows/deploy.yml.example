# Example: deploy services and apps
# Copy to deploy.yml and customize. Trigger on push to main or manual.

name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Deploy
        run: |
          # Example: build Docker images, run migrations, deploy to your environment
          # cd infrastructure/docker && docker-compose build
          # Run migrations, deploy to cloud, etc.
          echo "Add your deploy steps here"
